# Core CLI (research_person.py) uses only the Python standard library.
# The Streamlit chat UI (chat_researcher.py) needs these:
streamlit>=1.40
anthropic>=0.40        # optional: enables LLM extraction + Q&A
reportlab>=4.0         # render the downloadable report as a PDF
pillow>=9.2            # rasterize color emoji into the PDF (usually pulled in by streamlit)
# The NZ Outreach Researcher (outreach_researcher.py) additionally needs:
requests>=2.32         # Perplexity Agent API calls
pandas>=2.0            # candidate table + CSV export
